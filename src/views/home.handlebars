<u1>
    {{#each products}}
    <div class="accordion m-2" id="accordionPanelsStayOpen{{id}}">
        <div class="accordion-item">
            <h2 class="accordion-header" id="panelsStayOpen-h{{id}}">
                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                    data-bs-target="#panelsStayOpen-{{id}}" aria-expanded="true"
                    aria-controls="panelsStayOpen-{{id}}">
                    <strong>ID: {{id}} | {{title}}</strong> 
                </button>
            </h2>
            <div id="panelsStayOpen-{{id}}" class="accordion-collapse collapse"
                aria-labelledby="panelsStayOpen-h{{id}}">
                <div class="accordion-body">
                    <strong>Title: {{title}}</strong><br>
                    Description: {{description}}<br>
                    Price: <code>${{price}}</code><br>
                    Code: <code>{{code}}</code><br>
                    Status: {{status}}<br>
                    Stock: <code>{{stock}}</code><br>
                    Category: {{category}}<br>
                    Thumbnails:{{#each thumbnails}} {{this}};{{/each}}<br>
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</u1>